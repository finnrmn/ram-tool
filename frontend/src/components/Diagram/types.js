export const isComponentData = (data) => data.kind === "component";
export const isAndData = (data) => data.kind === "and";
export const isOrData = (data) => data.kind === "or";
export const isKofNData = (data) => data.kind === "kofn";
